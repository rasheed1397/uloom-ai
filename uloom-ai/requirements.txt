# Backend (SRS Section 7)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
email-validator>=2.2
sqlalchemy>=2.0
alembic>=1.13
asyncpg>=0.29
psycopg2-binary>=2.9
pgvector>=0.3

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# passlib 1.7.4 (latest release) probes bcrypt internals that were removed in
# bcrypt>=4.1, causing every hash_password()/verify_password() call to raise
# ValueError. Pin below that until passlib ships a compatible release.
bcrypt<4.1

# Cache / queue state
redis>=5.0

# AI - Google Gemini is the default provider (SRS Section 7, Detailed Design Section 5.5/5.6)
google-genai>=0.3

# Alternate AI adapters (optional, install as needed when swapped in)
# anthropic>=0.34
# openai>=1.40
# voyageai>=0.2

# RAG orchestration helpers
tiktoken>=0.7
